# ✅ ميزة الالتزامات - اكتملت!

## 🎯 الفكرة
ميزة "الالتزامات" تسمح للأزواج بإنشاء اتفاقيات مع عقوبات:
- **المسؤول:** من المسؤول عن الالتزام (مثلاً: يزن)
- **الالتزام:** ماذا يجب القيام به (مثلاً: الروحة على الجيم)
- **الهدف:** كم مرة (مثلاً: 5 مرات)
- **الفترة:** يومي / أسبوعي / شهري
- **العقاب:** ماذا يحدث إذا فشل (مثلاً: طبخ العشاء لمدة أسبوع)

---

## ✨ ما تم إضافته

### 1. قاعدة البيانات (SQL)
تم إضافة جدولين جديدين في `/supabase_schema.sql`:

#### جدول `commitments`
```sql
- id: معرف فريد
- partnership_id: ربط بالشراكة
- owner_user_id: المسؤول عن الالتزام
- title: عنوان الالتزام
- description: وصف
- target_count: الهدف (عدد المرات)
- period_type: الفترة (daily/weekly/monthly)
- punishment: العقاب
- start_date: تاريخ البداية
- end_date: تاريخ النهاية (اختياري)
- is_active: نشط أم لا
```

#### جدول `commitment_logs`
```sql
- id: معرف فريد
- commitment_id: ربط بالالتزام
- log_date: تاريخ اليوم
- is_completed: هل تم الإنجاز؟
- note: ملاحظة (اختياري)
```

### 2. الواجهة (UI)
تم إنشاء `/src/app/components/CommitmentsScreen.tsx` مع:
- ✅ عرض الالتزامات النشطة
- ✅ إحصائيات (نشطة، مكتملة، في خطر)
- ✅ شريط تقدم لكل التزام
- ✅ حالة الالتزام (على المسار الصحيح، في خطر، فشل، مكتمل)
- ✅ نموذج لإضافة التزام جديد
- ✅ أزرار سريعة (✓ تم اليوم، عرض التفاصيل)

### 3. التكامل
- ✅ أضيف زر "الالتزامات" في الصفحة الرئيسية
- ✅ أضيف أيقونة في الـ Bottom Navigation
- ✅ ربط بـ App.tsx للتنقل

---

## 📊 المميزات الحالية

### البيانات التجريبية
الآن التطبيق يعرض بيانات تجريبية:
- التزام واحد نموذجي (يزن - الجيم)
- التقدم: 3/5 مرات
- الحالة: على المسار الصحيح

### الإحصائيات
- 3 التزامات نشطة
- 12 التزام مكتمل
- 1 التزام في خطر

---

## 🔮 الخطوات القادمة (للربط مع Supabase)

### 1. إنشاء التزام جديد
```typescript
const { data, error } = await supabase
  .from('commitments')
  .insert([{
    partnership_id: '...',
    owner_user_id: '...',
    title: 'الروحة على الجيم',
    description: '5 مرات خلال الأسبوع',
    target_count: 5,
    period_type: 'weekly',
    punishment: 'طبخ العشاء لمدة أسبوع',
    start_date: '2026-01-08'
  }]);
```

### 2. تتبع الإنجاز اليومي
```typescript
const { data, error } = await supabase
  .from('commitment_logs')
  .insert([{
    commitment_id: '...',
    log_date: '2026-01-08',
    is_completed: true,
    note: 'تمرين رائع اليوم!'
  }]);
```

### 3. حساب التقدم
```typescript
// جلب سجلات الأسبوع الحالي
const { data: logs } = await supabase
  .from('commitment_logs')
  .select('*')
  .eq('commitment_id', commitmentId)
  .gte('log_date', startOfWeek)
  .lte('log_date', endOfWeek)
  .eq('is_completed', true);

const progress = logs?.length || 0;
```

### 4. تحديث الحالة
```typescript
if (progress >= targetCount) {
  status = 'completed';
} else if (daysLeft < 2 && progress < targetCount) {
  status = 'at-risk';
} else {
  status = 'on-track';
}
```

---

## 🎨 التصميم

### الألوان
- **البرتقالي:** للالتزامات (Target icon)
- **الأخضر:** للإنجاز والنجاح
- **الأحمر:** للعقوبات والفشل
- **الأزرق:** للالتزامات على المسار الصحيح
- **البرتقالي الفاتح:** للالتزامات في خطر

### الأيقونات
- 🎯 Target: الالتزامات
- 🏆 Trophy: الإنجازات
- ⚠️ AlertCircle: في خطر
- 📅 Calendar: الفترة الزمنية

---

## 📝 ملاحظات

1. **قاعدة البيانات جاهزة:** لازم تطبق ملف SQL المحدّث على Supabase
2. **البيانات التجريبية:** حالياً الواجهة تعرض بيانات وهمية
3. **الربط لاحقاً:** يمكنك ربط الـ CRUD operations مع Supabase
4. **التنبيهات:** يمكن إضافة تنبيهات عند الفشل أو النجاح

---

## 🚀 جاهز للاستخدام!

التطبيق الآن يحتوي على ميزة الالتزامات كاملة:
1. ✅ قاعدة البيانات محدّثة
2. ✅ الواجهة جاهزة ومصممة
3. ✅ التنقل شغال
4. ✅ البيانات التجريبية موجودة

**الخطوة التالية:** طبّق ملف SQL المحدّث على Supabase Dashboard وابدأ الاستخدام!
