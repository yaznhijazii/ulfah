# إعداد قاعدة بيانات Supabase لتطبيق أُلْفَة

## 🚨 مهم جداً - اتبع هذه الخطوات بالترتيب

### ⚠️ المشكلة الحالية
التطبيق الآن يعرض رسالة:
```
⚠️ يرجى إعداد Supabase أولاً. راجع ملف SUPABASE_SETUP_AR.md
```

هذا طبيعي! لأن Supabase غير متصل بعد. اتبع الخطوات التالية:

---

## 📋 الخطوات المطلوبة منك

### 1️⃣ إنشاء حساب Supabase (إذا لم يكن لديك)

1. اذهب إلى [https://supabase.com](https://supabase.com)
2. اضغط **Start your project**
3. سجل دخول بحساب GitHub الخاص بك
4. اضغط **New project**
5. اختر اسم للمشروع (مثلاً: ulfah-app)
6. اختر كلمة مرور قوية لقاعدة البيانات
7. اختر المنطقة الأقرب لك
8. اضغط **Create new project**
9. انتظر دقيقة حتى يتم إنشاء المشروع

### 2️⃣ الحصول على مفاتيح API

1. في Supabase Dashboard، اذهب إلى **Settings** (⚙️) من القائمة الجانبية
2. اختر **API** من القائمة الفرعية
3. ستجد:
   - **Project URL** (مثل: https://xxxxx.supabase.co)
   - **anon public** key (نص طويل يبدأ بـ eyJ...)

**احتفظ بهذه القيم! ستحتاجها في الخطوة التالية**

### 3️⃣ تطبيق SQL Schema على قاعدة البيانات

1. في Supabase Dashboard، اذهب إلى **SQL Editor** (📝) من القائمة الجانبية
2. اضغط **New query**
3. افتح ملف `/supabase_schema.sql` من المشروع
4. انسخ **كامل المحتوى** (كل الـ 249 سطر)
5. الصق في SQL Editor
6. اضغط **Run** (أو Ctrl/Cmd + Enter)
7. انتظر حتى تظهر رسالة **Success!**

✅ الآن تم إنشاء جميع الجداول في قاعدة البيانات!

### 4️⃣ إضافة مفاتيح API للتطبيق

**في Figma Make:**

لسوء الحظ، Figma Make لا تدعم ملفات `.env` مباشرة. لذلك لديك خياران:

#### الخيار الأول (الموصى به): استخدام أداة الربط

استخدم الزر "Connect to Supabase" في التطبيق إذا كان متوفراً.

#### الخيار الثاني: تعديل الملف مباشرة (مؤقت)

قم بتعديل ملف `/src/lib/supabase.ts` مباشرة:

```typescript
// استبدل هذه الأسطر:
const supabaseUrl = import.meta.env.VITE_SUPABASE_URL || 'https://placeholder.supabase.co';
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY || 'eyJhbGc...';

// بهذا (ضع قيمك الحقيقية):
const supabaseUrl = 'https://YOUR_PROJECT_ID.supabase.co';
const supabaseAnonKey = 'YOUR_ANON_KEY_HERE';
```

⚠️ **تحذير:** لا تشارك هذا الكود مع أحد إذا وضعت مفاتيحك الحقيقية!

---

## 🧪 الاختبار

بعد تطبيق الخطوات أعلاه:

1. ✅ جرب إنشاء حساب جديد
2. ✅ افحص Supabase Dashboard → Table Editor → `users` 
3. ✅ تأكد من ظهور المستخدم الجديد مع كلمة مرور مشفرة
4. ✅ جرب تسجيل الدخول بنفس الحساب

## 🔐 ملاحظات أمنية

- ✅ كلمات المرور مشفرة باستخدام bcrypt
- ✅ لا يتم حفظ كلمات المرور النصية أبداً
- ⚠️ تأكد من عدم مشاركة `.env.local` في Git
- ⚠️ قم بإعداد RLS policies قبل الإنتاج

## 🎯 الخطوات التالية

بعد نجاح المصادقة، يمكنك:
1. ربط الشريك باستخدام كود الربط
2. عرض وحفظ المشاعر اليومية
3. إضافة وعرض الذكريات
4. إدارة التقويم والأحداث
5. إضافة وجهات السفر

---

## ⚡ الكود الحالي

### AuthSignup.tsx
```typescript
- يتحقق من عدم وجود اسم المستخدم
- يشفّر كلمة المرور باستخدام bcrypt
- يحفظ المستخدم الجديد في قاعدة البيانات
- يسجل الدخول تلقائياً بعد التسجيل
```

### AuthLogin.tsx
```typescript
- يبحث عن المستخدم في قاعدة البيانات
- يتحقق من كلمة المرور المشفرة
- يعيد user_id عند النجاح
```

## 📞 المساعدة

إذا واجهت أي مشاكل:
1. تأكد من تطبيق SQL schema بنجاح
2. تحقق من متغيرات البيئة
3. افحص console في المتصفح للأخطاء
4. تحقق من Supabase Logs في Dashboard

---

🎉 **التطبيق الآن جاهز للاستخدام الحقيقي!**